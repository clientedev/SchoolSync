{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0:$PORT final_railway_app:app --timeout 120 --workers 1 --max-requests 1000 --log-level info",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}